apiVersion: v1
kind: Service
metadata:
  name: {{ include "dreamteams_api.fullname" . }}
  labels:
    {{- include "dreamteams_api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dreamteams_api.selectorLabels" . | nindent 4 }}
