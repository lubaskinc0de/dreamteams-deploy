apiVersion: v1
kind: Service
metadata:
  name: {{ include "dreamteams_frontend.fullname" . }}
  labels:
    {{- include "dreamteams_frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dreamteams_frontend.selectorLabels" . | nindent 4 }}
