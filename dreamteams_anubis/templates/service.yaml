apiVersion: v1
kind: Service
metadata:
  name: {{ include "dreamteams_anubis.fullname" . }}
  labels:
    {{- include "dreamteams_anubis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "dreamteams_anubis.selectorLabels" . | nindent 4 }}
